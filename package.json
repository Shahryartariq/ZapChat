{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "scripts": {
    "start": "cross-env PROD=true node server.js",
    "dev": "concurrently \"cd client && yarn start\" \"cross-env PROD=false node server.js\"",
    "postinstall": "cd ./client && yarn"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0"
  }
}
